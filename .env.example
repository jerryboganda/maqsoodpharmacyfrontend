# Base URL of the pharmacy-platform NestJS API (see the sibling `rebuild` repo, apps/api).
# Must be prefixed PUBLIC_ to be readable from client-side code (SvelteKit/Vite convention) --
# never put a secret behind a PUBLIC_ var, it ships in the client bundle.
PUBLIC_API_BASE_URL=http://localhost:3001
